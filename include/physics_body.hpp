#ifndef PHYSICS_BODY_HPP
#define PHYSICS_BODY_HPP

class PhysicsBody
{
    //All other bodies will extend from this
    //Base attributes such as position (Vector), velocity (Vector), is_affected_by_gravity (Bool)
    //Methods such as update(), 
};

#endif